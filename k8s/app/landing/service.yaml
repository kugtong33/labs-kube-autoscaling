apiVersion: v1
kind: Service
metadata:
  name: sample-app
  labels:
    app: autoscaling-lab
    mode: landing
spec:
  type: NodePort
  selector:
    app: autoscaling-lab
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
